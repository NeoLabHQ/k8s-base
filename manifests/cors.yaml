apiVersion: configuration.konghq.com/v1
kind: KongPlugin
plugin: cors
metadata:
  name: omnigram-cors
config: 
  origins:
  - (^|^[^:]+:\/\/|[^\.]+\.)omnigram\.ru # -> actually means *.omnigram.ru
  
  exposed_headers:
  - "*"
  credentials: true
  max_age: 3600
  preflight_continue: false
